war(웹기반 압축파일)
jar(자바 기반 압축파일)

스프링
프레임워크, 보통 무거운데 스프링은 경량 프레임워크이다.

Spring Tool Suite 3 다운로드
- Spring Legacy Project 를 지원하는 이클립스 기반 IDE
- 주소창에 spring.io 입력
스프링 부트(4버전 이용) STS4버전
스프링 프레임워크(레거시 프로젝트) STS3버전 이거 두개 이용하면됨

spring tool4 접속(STS3 버전을 다운로드 받기위해서)
맨 아래로 내려가면 3버전링크있음

들어가면 이클립스 4.19버전 다운받으면 됨
첫번째꺼 다운
이클립스 버전 확인 하는법(help-> about 머시기 )

전체적인 기능을 제공 하나의 프로그램 (프레임워크=spring), 일부 기능만 제공(라이브러리)

처음 워크스페이스를 만들면 다른 서버가 들어가 있는데 이거말고
그대로 톰캣을 쓸것이다.
하단 서버창에 우클릭 new 서버 하고 톰캣으로 설정하면 된다 ( 넥스트 자주 누르면서)
그리고 나머지 서버는 지우면 된다 
server.xml 들어가서 포트번호를 9090으로 바꿔준다.

퍼스펙티브 들어가서 기본 설정을 스프링으로 맞춰서 쓰면 된다.
(프리퍼런스 들어가서 원래 하는 설정 다 맞춰주면 됨)

스프링에서는 contextPath(대표이름)가 프로젝트이름이 아니다
사용할 프로젝트가 달라지기 때문에 spring legacy project로 바뀌어진다.
이걸 이용해서 maven 프로젝트를 실행시킨다

STS3는 프로젝트용

프로젝트 만들때 spring MVC project 선택
설정하고 넥스트 누르면 패키지 설정 잘 했냐고 확인한다.

톰캣에서는 같은 프로젝트가 생성되면 안된다 고로 스프링에서도 계속 contextPath
가 계속 달라져야 한다.

src/main/java
내부에 HomeController 삭제하기(디폴트값으로 들어간 파일임)
평소대로 패키지를 만드는 방식은 이제 스프링에서는 안먹힌다 그이유는 상단에도 
있음
앞으론느 패키지 생성할때는 초반에 베이스로 설정한 패키지에다가 뉴 패키지를 하면 된다
그럼 아래 상단에 이름 그대로 따서 패키지가 새롭게 만들어진다.
클래스 만들때는 따로 제약사항은 없다

콘솔이랑 서버 있는 하단 탭 Namespaces 들어가면 밑에서 3번째 p 라고 써져 있는거 기능이 프로퍼티도 속성으로 쓸수 있게 해준다. 

xml 문서내에서 계산기능이 필요할때는 자바코드를 이용할수 있다. 자바로 
bean파일을 만든다.

IoC container 에다가 객체(Bean)을 spring bean configuration file을 만든다
 
GenericXmlApplicationContext를 이용해 만들어진 bean을 getBean("id")을 이용해 가져다가 쓴다.

@Configuratuon
@Bean
을 이용해서 자바코드로 만들어 줄수도 있다.

oracle을 이용해서 ojdbc6.jar을 이용해서 dao를 만들어줄수 있다
근데 ojdbc8.jar 

자르 파일을 추가하는 방법은 dependency 태그로 추가한다.


윈도우 프리퍼런스 콘솔검색 80000글자 제한 해제하자

war 파일을 만들어서 톰캣에서 배포와 빌드

우리가 하는 방식인
legacy프로젝트도 구식이다
요즘은 boot

동적값을 리스트 파일이 아닌 첫화면은 웰컴파일로 설정해놓으면
아무런 기능을 못한다. 웰컴파일을 직접 작성해야한다 
그래야 값을 가지고 갈일이 있으면 처리가 된다.












